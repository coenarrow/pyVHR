import pandas as pd

def create_instance_dataframe(data_dir,results_path):
    """
    Create or load a DataFrame with instance-related information.

    Parameters:
        results_path (str): The path to save the DataFrame.
        data_dir (str): The directory containing all videos.
        
    Returns:
        pd.DataFrame: DataFrame containing instance-related information.
    """

    # Add a check to see if the DataFrame already exists.
    pass

